datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}


// Modelo de Produtos
model Product {
  id    String  @id @default(uuid())
  name  String
  price Float
  stock Int
  slug  String  @unique

}